# Book of the Month - Native script

## Installation

Be sure to have `native-script` installed.

Install dependencies:

```
tns install
```

## Development

Run on iOS simulator:

```
tns run ios --emulator
```
